/*
14. Longest Common Prefix
Write a function to find the longest common prefix string amongst an array of strings.

If there is no common prefix, return an empty string "".
*/
#include<iostream>
#include<vector>
#include<string>
#include<string_view>

std::string answer(std::vector<std::string> sample){
    std::string answer = sample[0];//take the first word

    for(int idx = 1; idx < sample.size()-1; idx++){
        
    }
    //substring for the common prefix
    //if string.size == 0 return "" else continue
}
int main(){
    std::vector<std::string> sample;
    std::string answer = "";

    return 0;
}